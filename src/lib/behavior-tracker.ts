/**
 * behavior-tracker.ts
 * Stub for deferred client-side behaviour tracking.
 * Loaded lazily via dynamic import in AnalyticsProvider after mount.
 */

export function initBehaviorTracking(): void {
  // No-op stub — extend with real tracking logic when needed.
}
